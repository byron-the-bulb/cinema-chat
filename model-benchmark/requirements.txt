# WhisperX — faster-whisper (CTranslate2) + forced word alignment
# CTranslate2 uses INT8 quantization on CPU: fast even without a GPU
whisperx>=3.1.0

# Qwen3-ASR — official package (includes Qwen3-ForcedAligner for timestamps)
# https://huggingface.co/Qwen/Qwen3-ASR-1.7B
qwen-asr>=0.1.0

# PyTorch — base install (CPU bfloat16 works on Zen5/AVX-512).
# For AMD ROCm GPU acceleration, swap this wheel:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/rocm6.1
torch>=2.4.0
torchaudio>=2.4.0

# Audio I/O
soundfile>=0.12.1
librosa>=0.10.2

# Optional: Word Error Rate calculation (--ref flag)
jiwer>=3.0.0

# Optional: better memory accounting (falls back to resource.getrusage if absent)
psutil>=5.9.8
